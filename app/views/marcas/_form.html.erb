<%= form_for(@marca) do |f| %>
  <% if @marca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@marca.errors.count, "error") %> prohibited this marca from being saved:</h2>

      <ul>
      <% @marca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="field">
      <%= f.label :name, "Marca *", :class => "bold" %><br />
      <%= f.text_field :name %>
    </div>
    
<div class="field">
      <%= f.label :customer_id , "Cliente", :class => "bold" %><br />
      <%= f.collection_select :customer_id, @customers, :id, :name,{:prompt   => "Seleccione un cliente "} %>
      </div>


  <div class="actions">
    
    <%= f.submit %>
  </div>
<% end %>
