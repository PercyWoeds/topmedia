json.extract! stamentacount, :id, :bank_account_id, :fecha1, :fecha2, :saldo_inicial, :saldo_final, :user_id, :company_id, :created_at, :updated_at
json.url stamentacount_url(stamentacount, format: :json)
