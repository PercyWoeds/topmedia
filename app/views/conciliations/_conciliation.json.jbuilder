json.extract! conciliation, :id, :fecha1, :fecha2, :saldo_inicial, :saldo_final, :user_id, :company_id, :bank_acount_id, :created_at, :updated_at
json.url conciliation_url(conciliation, format: :json)
